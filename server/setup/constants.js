const path = require('path');

const directory = {};

// dist
directory.DIST_NAME = 'dist';
directory.DIST_ROUTE = '/' + directory.DIST_NAME;

directory.PUBLIC_NAME = 'public';


module.exports = directory;